# Lambda containment function has been moved to terraform-lambda/
# It uses a separate Terraform state and references this EKS cluster via data source.
# Deploy it from the dashboard "Cortex - Response" section.
